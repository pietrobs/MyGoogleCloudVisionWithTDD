const resultPath = 'results';
const responsePath = 'responses';
const examplesPath = 'examples';
const examplesTestPath = 'examples/tests';
const responseTestPath = 'responses/tests';

const detectionTypes = {
    FACE: 'face',
    OBJECT: 'object',
    TEXT: 'text',
    PROPERTIES: 'properties',
}

module.exports = {resultPath, responsePath, examplesPath, examplesTestPath, responseTestPath, detectionTypes}